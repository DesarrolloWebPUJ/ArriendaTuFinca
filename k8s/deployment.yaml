apiVersion: apps/v1
kind: Deployment
metadata:
  name: arriendatufinca-deployment
  namespace: arriendatufinca
spec:
  selector:
    matchLabels:
      app: arriendatufinca-deployment
  replicas: 1
  template:
    metadata:
      labels:
         app: arriendatufinca-deployment
    spec:
      imagePullSecrets:
         - name: github-secret
      containers:
         - name: estudiante
           image: localhost:5000/arriendatufinca:latest
           ports:
              - containerPort: 8080
           
---
apiVersion: v1
kind: Service
metadata:
  name: arriendatufinca-service
  namespace: arriendatufinca
spec:
  type: NodePort
  selector:
    app: arriendatufinca-deployment
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080